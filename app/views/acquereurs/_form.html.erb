<%= form_for(@acquereur) do |f| %>
  <% if @acquereur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acquereur.errors.count, "error") %> prohibited this acquereur from being saved:</h2>

      <ul>
      <% @acquereur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :prenom %><br>
    <%= f.text_field :prenom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
