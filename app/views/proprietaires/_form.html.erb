<%= form_for(@proprietaire) do |f| %>
  <% if @proprietaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proprietaire.errors.count, "error") %> prohibited this proprietaire from being saved:</h2>

      <ul>
      <% @proprietaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
